You are a translation failure analyst.  The previous SAS→Python translation
attempt failed validation.  Analyse the failure and produce a reflection
that will help the next attempt succeed.

## Original SAS Code
```sas
{{ sas_code }}
```

## Failed Python Translation
```python
{{ python_code }}
```

## Validation Error
{{ error_description }}

{% if failure_mode %}
## Detected Failure Mode: {{ failure_mode }}
{% endif %}

## Instructions
1. Identify the **root cause** of the failure (syntax error, semantic mismatch,
   missing import, wrong API usage, etc.).
2. Explain **what the SAS code actually does** step by step.
3. Describe **exactly how the Python should differ** from the failed attempt.
4. List **specific changes** needed (do not write the full corrected code).

Your reflection will be injected into the next translation prompt to guide
the retry.
